---
name: advise
description: 行き詰まったとき、アプローチの決定前、完了宣言前などの判断ポイントで、強いモデルの advisor subagent に相談してセカンドオピニオンを得る。Use when アドバイスが欲しい、相談したい、セカンドオピニオン、行き詰まった、方針の妥当性確認、完了前チェックを求められた際に使用する。
argument-hint: '[相談内容 (省略時は現在のセッションの問題から自動構成)]'
allowed-tools:
  - Bash
  - Read
  - Edit
  - Write
  - Glob
  - Grep
  - Task
  - SendMessage
  - AskUserQuestion
---

# Advisor 相談ワークフロー

判断ポイントで `dev:advisor` subagent (fable) に相談し、独立した診断と助言を得て作業に反映する。ネイティブの advisor tool (後述の「ネイティブ advisor tool との関係」を参照) と異なり advisor は会話履歴を自動では見られないため、**自己完結した相談ブリーフの構成が品質を決める**。

## 相談すべき判断ポイント

- **アプローチ決定前**: 大きな変更・リファクタリングの方針にコミットする前
- **エラーの再発時**: 同種のエラーが 2〜3 回の修正でも解決しないとき
- **完了宣言前**: 大きなタスクの完了を報告する前の独立チェック
- **選択肢の比較**: 設計上のトレードオフで判断が割れるとき

短いタスクや自明な判断では相談しない (コストに見合わない)。

**`dev:review` / `dev:feedback` との違い:** `dev:review` は差分を多角的にレビューして自動修正まで行い、`dev:feedback` は外部からの指摘の取捨選択を行う。`dev:advise` はどちらでもなく、方針・判断そのものへのセカンドオピニオンを得る手段で、advisor 自身はコードを変更しない (適用するかどうかは実行役の判断)。

## 実行手順

### 1. 相談内容の特定

- **引数あり**: 引数を相談の主題とする
- **引数なし**: 現在のセッションで直面している問題 (直近のエラー、検討中の方針、完了予定の作業) から主題を構成する。特定できない場合は AskUserQuestion で確認する

### 2. 相談ブリーフの構成

以下を含む自己完結したブリーフを作る。advisor はセッションの会話を知らない前提で書く:

```markdown
## 相談の種類

<アプローチ評価 / エラー診断 / 完了前チェック / 選択肢の比較>

## ゴール

<最終的に達成しようとしていること>

## 現状

<今どこまで進んでいるか。関連ファイルのパスを含める (advisor は自身でコードを確認できる)>

## 検証済みの事実 (あれば)

- <裏取り済みの事実> (出典: <確認したドキュメント、コマンド結果等>)

## 試行履歴 (エラー診断の場合)

1. <試したこと> → <結果・エラー内容 (要点)>

## 制約

<技術的制約、方針、スコープの境界>

## 質問

<advisor に答えてほしいことを具体的に。「どう思うか」ではなく「A 案の見落としはあるか」のように絞る>
```

**重要:** ブリーフには自身の仮説や結論を「事実」として書かない。仮説は仮説と明示する (advisor の独立性を損なわないため)。逆に、裏取り済みの事実は出典と併せて「検証済みの事実」に明示する。advisor は WebFetch を持たず外部情報を再検証できないため、出典がないと検証済みの事実にも慎重側の判定をする。

### 3. advisor の起動

```
Task({
  subagent_type: "dev:advisor",
  name: "advisor-<連番>",
  description: "<相談の要約>",
  prompt: <構成したブリーフ>
})
```

`name` は手順 5 の SendMessage による追加相談の宛先になるため必ず付与する。同一セッションで複数回起動する場合は連番等で一意にする。

advisor は自身で関連コードを確認した上で、診断・推奨・代替案・リスク・見落としを返す。バックグラウンドで実行されるため、助言は完了時のメッセージとして届く。助言の受領後に advisor から idle 通知が届くことがあるが、対応は不要。

**フォールバック:** `dev:advisor` が起動できない場合は `general-purpose` subagent に `model` パラメータで上位モデル (fable、利用不可なら opus) を指定し、ブリーフに「助言のみ返す。実装は行わない」と advisor の助言フォーマット (診断 / 推奨 / 代替案 / リスク) を含める。

### 4. 助言の適用

助言を鵜呑みにせず、自身の証拠と照合してから適用する:

- 助言が現在のコード・事実と整合する → 適用する
- 推奨されたステップが実際に失敗する、または事実と矛盾する → 盲従せず、矛盾点を明確にして次のステップ (追加相談 or ユーザーへの報告) を判断する
- 適用の判断とその理由を記録し、最終報告に含める

### 5. 追加相談 (必要な場合)

助言に不明点がある、適用結果が想定と異なる場合は、**同じ advisor に SendMessage で追撃する** (v2.1.198+ の subagent 再開機能により、コンテキストを保持したまま多ターン相談できる):

```
SendMessage({ to: "<手順 3 で付与した name>", ... })
```

SendMessage による subagent の再開が使えない環境では、前回の助言・適用結果・新たな疑問点をブリーフに含めた新しい Task 起動で相談する。

新しい論点の相談は新しい Task 起動で行う (古いコンテキストの混入を避ける)。

### 6. 報告

```markdown
## 相談結果

### 診断・助言の要点

<advisor の診断と推奨の要約>

### 適用した内容

- <助言に基づいて実施した変更・判断>

### 適用しなかった助言と理由

- <あれば。なければ「なし」>

### 次のステップ

- <残作業、ユーザーの判断が必要な点>
```

## ネイティブ advisor tool との関係

Claude Code v2.1.98+ (Anthropic API) には `/advisor` コマンドで有効化できるネイティブの advisor tool があり、会話全文を自動で advisor に渡せる。常時 advisor を使いたい場合はそちらを推奨する。このスキルは、advisor tool が使えない環境 (Bedrock 等) や、相談のタイミング・内容を明示的に制御したい場合の代替手段。
